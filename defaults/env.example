##  NOTE: This is an example file. DO NOT MODIFY IT!!  ##
##     For runtime changes, copy-to and edit: .env     ##
##     For details, see docs/project-structure.md      ##

SIM_DEPTH=1       # simulated hours. 720H ≈ 1M, 8760H ≈ 1Y
SIM_ROUNDS=1      # [integer|sweep]
WORKERS=1
WORKER_RAM=2048   # MB

DATA_MODE=full    # [simple|metrics|full]. package.json provides faster switch
LOG_MODE=         # info,probe,stats (or blank). package.json provides faster switch

SEED=1            # Seed for reproducibility
